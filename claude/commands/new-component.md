Create a new component. The component name is: $ARGUMENTS

Before writing code, follow these steps:

1. Determine where the component belongs based on its purpose and the project's directory structure.

2. Read 2-3 existing components in the target directory to match conventions:
   - Import paths and aliases
   - UI primitive usage (shadcn/ui, or the project's component library)
   - Icon library (lucide-react, heroicons, etc.)
   - State management patterns (Zustand, nanostores, context, etc.)
   - Styling approach (Tailwind, CSS modules, etc.)

3. Create the component file following the patterns you observed.

4. If the project uses a component library with barrel exports, add the new component to the relevant index file.

5. Run the project's type check command to verify 0 errors.

<!-- Customize for your project:
- Specify the exact directories for different component types
- Specify the UI primitives and icon library
- Specify whether stories/tests are required alongside components
- Specify any naming conventions (PascalCase files, kebab-case folders, etc.)
-->
