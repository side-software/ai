---
description: Repo map, stack, commands, and key file locations for {{PROJECT_NAME}}. Always-on context for every conversation.
activation: always_on
---

# Project Overview

**Stack**: {{STACK_DESCRIPTION}}

**Package manager**: {{PACKAGE_MANAGER}}

**Key locations**:
- {{Key config}}: `{{path}}`
- {{State management}}: `{{path}}`
- {{App entry point}}: `{{path}}`
- {{Data pipeline}}: `{{path}}`

**Commands**:
- Dev: `{{DEV_COMMAND}}`
- Build: `{{BUILD_COMMAND}}`
- Test: `{{TEST_COMMAND}}`
- Verify: `{{VERIFY_COMMAND}}`

**API**: {{API_URL}} — inspect responses when debugging.

**Skills**: Read `.agents/skills/project-conventions/SKILL.md` before any non-trivial change.
