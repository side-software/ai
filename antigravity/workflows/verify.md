# Verify

Run the full verification suite for this project.

## Steps

1. Run the type check command for this project.
2. Run the build command.
3. Run the test command.
4. Report the result of each step. If any step fails, stop and report the failure with details.

<!-- Customize for your project:
Example for Astro: npx astro check && npx astro build && npm test
Example for pnpm monorepo: pnpm verify
Example for Python: ruff check . && mypy . && pytest
-->
